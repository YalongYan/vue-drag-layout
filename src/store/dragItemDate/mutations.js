export default{
  updateLayoutContentItem (state, item) {
    // state.formData = {...state.formData, ...payload}
    console.log(state)
    console.log(item)
  }
  // updateFormInfo (state, payload) {
  //   state.formInfo = {...state.formInfo, ...payload}
  // }
}
